[project]
name = "civic-flag"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.124.4",
  "pdfplumber==0.11.7",
  "python-docx==1.2.0",
  "python-multipart==0.0.20",
]

[tool.vercel]
entrypoint = "api.index:app"
